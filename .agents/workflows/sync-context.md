# Workflow: Sync Chat Context

## Execution
Whenever the user runs the command `/sync-context` or when wrap-up tasks are initialized:
1. **Analyze**: Read current changes and chat history in the current thread.
2. **Update**: Open [.antigravity-context](file:///c:/Users/DELL/Desktop/Coded/intisor/TurnIt/.antigravity-context) and modify the `Active Roadmap Checklist` and relevant section descriptions to reflect the current status.
3. **Save**: Persist the file.
